# Provenance Status

Historical GARVIS records refer to `ProCityHub/hypercubeheartbeat`.

Until a verified current mapping exists, treat that identifier as historical or unresolved
provenance, not as a live dependency or scientific evidence.

A current dependency should use a verified current repository reference and, where practical,
an immutable 40-character Git commit SHA.

Do not silently rewrite historical retractions. Do not convert dead links into evidence.
