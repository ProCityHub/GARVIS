name: Issue Management

on:
  issues:
    types: [opened, labeled]

permissions:
  issues: write

jobs:
  triage:
    runs-on: ubuntu-latest
    # Only run when there is a real issue payload
    if: github.event.issue != null
    steps:
      - name: No-op triage
        run: echo "Issue triage placeholder — no action required."
